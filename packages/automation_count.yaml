template:
  - sensor:
      - name: Number of running automations
        unique_id: automations_currently_running
        state: >-
          {{ states.automation | selectattr("attributes.current", "defined") | map(attribute="attributes.current") | sum }}
        unit_of_measurement: "Automations"
