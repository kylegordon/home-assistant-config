alias: Kyle recognised
trigger:
  platform: event
  event_type: image_processing.detect_face
  event_data:
    name: 'Kyle Gordon'
action:
  - service: notify.email_kyle
    data_template:
      message: Kyle Gordon recognised with probability {{ trigger.event.data.confidence }}
      title: Face notification