---
packages:
  common: !include common/common.yaml

substitutions:
  device_name: bluetooth_proxy_1
  device_description: Bluetooth Proxy 1
  friendly_name: Bluetooth Proxy 1

esphome:
  name: ${device_name}
  comment: ${device_description}
  platform: ESP32
  board: nodemcu-32s

captive_portal:

logger:

web_server:

time:
  - platform: homeassistant
    id: esptime
    timezone: Europe/London

esp32_ble_tracker:
  scan_parameters:
    interval: 1100ms
    window: 1100ms
    active: true

bluetooth_proxy:
