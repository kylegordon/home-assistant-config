---
# Use ESPHome-Flasher, esptool.py doesn't do required magic with the bootloader
# See https://github.com/esphome/issues/issues/598

# Do not enable BLE tracking when the camera module is enabled.
# Device will not boot correctly

substitutions:
  device_name: esp_camera_1
  device_description: ESP32 Camera 1
  friendly_name: ESP32 Camera 1

<<: !include common/esp32_camera_common.yaml
